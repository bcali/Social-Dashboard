name = "gamma-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
ALLOWED_ORIGIN = "https://__GITHUB_USERNAME__.github.io"

# GAMMA_API_KEY is set via: npx wrangler secret put GAMMA_API_KEY
